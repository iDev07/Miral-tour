import Hero from "@/components/Home/hero";
import React from "react";

function Search({ cities, tourpackages, categories }) {
  return (
    <div className="Search">
      {/* <Hero
        categories={categories}
        cities={cities}
        tourpackages={tourpackages}
      /> */}
    </div>
  );
}

export default Search;
